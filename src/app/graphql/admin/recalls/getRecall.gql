query getRecall($token: String!, $_id: String!) {
	admin(token: $token) {
		recall(_id: $_id) {
			_id
			question
			explanation
			concept {
				_id
				name
			}
			category {
				_id
				name
			}
			topic {
				_id
				name
			}
			linked_question
		}
	}
}
